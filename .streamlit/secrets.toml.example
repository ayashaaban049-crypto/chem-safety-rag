# Copy this file to .streamlit/secrets.toml for local runs (and add that
# real file to .gitignore), or paste these values into
# Streamlit Cloud -> your app -> Manage app -> Secrets.
#
# NEVER commit a real key. This example file contains no real key.

GROQ_API_KEY = "your_groq_key_here"
GROQ_MODEL = "llama3-70b-8192"
